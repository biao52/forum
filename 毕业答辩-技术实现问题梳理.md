# 毕业答辩 —— 老师可能问到的技术实现问题（10个）

> 项目：基于 Spring Boot 的论坛系统（前后端分离）
> 技术栈：Spring Boot 2.7.6 + MyBatis + MySQL + Redis + JWT + MinIO + Dify AI

---

## 1. JWT 认证机制：为什么密钥硬编码在代码中？令牌过期后如何处理？

**老师可能的问法：** 你的 JWT 密钥直接写在 `JwtUtil.java` 里，而且是静态常量，这样做有什么安全隐患？如果令牌被盗用怎么办？

**涉及代码：** [JwtUtil.java:19](src/main/java/com/yb/forum/utils/JwtUtil.java#L19)、[LoginInterceptor.java:48](src/main/java/com/yb/forum/interceptor/LoginInterceptor.java#L48)

**答辩要点：**
- 密钥硬编码的问题：反编译可获取，生产环境应使用配置中心或环境变量注入（项目中 dev/prod 的 yml 里已经配置了 `jwt.key`，但 `JwtUtil` 并未读取，这是可以改进的地方）
- 项目采用 HS512 签名算法，密钥长度 64 字节满足要求
- 令牌有效期 24 小时，到期后前端需跳转登录页
- 未实现 refresh token 机制，令牌过期即需重新登录
- 拦截器 `LoginInterceptor` 仅验证令牌有效性，未校验用户状态变更（如被禁言/删除后令牌仍可用）

---

## 2. 密码存储：为什么选择 MD5(md5(pwd) + salt) 而不是 bcrypt？

**老师可能的问法：** 你使用了 `MD5Util.md5Salt()` 来加密密码，为什么不使用更安全的 bcrypt 或 scrypt？MD5 在密码学上已经不再安全了你知道吗？

**涉及代码：** [MD5Util.java:28-30](src/main/java/com/yb/forum/utils/MD5Util.java#L28-L30)

**答辩要点：**
- 项目采用 `MD5(MD5(明文) + 随机盐值)` 的双重哈希 + 盐值方案，相比纯 MD5 有提升
- 但仍不具备抵御暴力破解的能力（MD5 计算速度太快），bcrypt 内置 work factor 可以调节计算成本
- Spring Security 提供了 `BCryptPasswordEncoder`，集成成本很低
- MD5 已于 2004 年被王小云教授证明存在碰撞攻击，不推荐在安全场景使用
- 答辩时可承认这是为了快速实现，后续应升级为 bcrypt

---

## 3. 点赞/浏览量的并发问题：先查后改模型在并发下有什么问题？

**老师可能的问法：** 你在 `thumbsUpById` 里先 `selectByPrimaryKey` 拿到 `likeCount`，再加 1 写入。如果两个用户同时点赞会怎样？

**涉及代码：** [ArticleServiceImpl.java:224-261](src/main/java/com/yb/forum/services/impl/ArticleServiceImpl.java#L224-L261)

**答辩要点：**
- "查询 → 加 1 → 更新" 是典型的 **read-modify-write** 竞态条件
- 两个并发请求可能读到相同的 `likeCount` 值（比如都是 10），各自写回 11，导致丢失一次更新
- **解决方案：**
  1. 使用数据库原子操作：`UPDATE article SET like_count = like_count + 1 WHERE id = ?`
  2. 使用 Redis 的 `INCR` 指令先计数，再异步同步到 MySQL
  3. 使用乐观锁（添加 version 字段）
- 同样的问题也存在于：`addOneVisitCountById`、`addOneReplyCountById`、`addOneArticleCountById`

---

## 4. Redis 缓存策略：缓存与数据库如何保证一致性？

**老师可能的问法：** 用户信息先查 Redis，查不到才查数据库然后回写 Redis。如果用户修改了信息，缓存怎么更新？会不会读到脏数据？

**涉及代码：** [UserController.java:210-227](src/main/java/com/yb/forum/controller/UserController.java#L210-L227)、[UserController.java:401-403](src/main/java/com/yb/forum/controller/UserController.java#L401-L403)

**答辩要点：**
- 项目采用 **Cache-Aside** 模式：查询时先读缓存，未命中则读 DB 回写；更新时写 DB 后更新/删除缓存
- 在 `modifyInfo`、`modifyPwd`、`logout` 等操作后有缓存更新/清除逻辑
- 但存在不一致窗口：更新 DB 成功 → 更新 Redis 失败 → 缓存中是旧数据（没有使用 MQ 重试或 canal 订阅 binlog）
- 管理员禁言/删除用户时有清除多级缓存（`article:list:*`），但没有做更细粒度的缓存失效
- TTL 设为 30 分钟作为兜底，最多 30 分钟内读到旧数据

---

## 5. 敏感词过滤的缺陷：基于字符串包含匹配有什么不足？

**老师可能的问法：** 你的敏感词过滤只是 `content.contains(sensitiveWord)`，如果用户用拼音、拆字、谐音绕过怎么办？另外为什么把含 `<>"'&` 的内容也判定为敏感？

**涉及代码：** [SensitiveFilterUtil.java:44-57](src/main/java/com/yb/forum/utils/SensitiveFilterUtil.java#L44-L57)

**答辩要点：**
- 当前实现为简单的子串匹配，存在以下问题：
  1. **误判率高**：所有含 `<>"'&[](){}` 的内容都被判定为"包含敏感词"，这会把大量正常的 Markdown 内容（链接 `[text](url)`、代码块）误判
  2. **绕过容易**：空格隔开、拼音、拆字、谐音均可绕过
  3. **词库硬编码**：不可动态更新，实际项目需要支持热加载词库
- 改进方向：基于 DFA 算法（确定有穷自动机）实现的多模式匹配，如 Hutool 的 `SensitiveUtil` 或自建 AC 自动机
- 对于特殊字符的过滤应该放在 XSS 防护层面，不应与敏感词混淆

---

## 6. XSS 防护：前端富文本编辑器的输入如何保证安全？

**老师可能的问法：** 你的论坛使用了 Editor.md 支持 Markdown，用户输入的是 HTML。你在哪里做了 XSS 过滤？够不够？

**涉及代码：** [XssUtil.java:25-35](src/main/java/com/yb/forum/utils/XssUtil.java#L25-L35)、[ArticleController.java:82](src/main/java/com/yb/forum/controller/ArticleController.java#L82)

**答辩要点：**
- 项目使用了 Jsoup 的 `Whitelist.basicWithImages()` 作为白名单过滤，允许基本格式化标签和图片，这是正确的做法
- XSS 防护应多层面实施：
  1. **输入过滤**（当前已做）：后端接收时用 Jsoup 清理
  2. **输出编码**：前端展示时应做 HTML 转义（检查 `th:text` / `v-html` 的使用）
  3. **CSP 头**：设置 `Content-Security-Policy` 响应头（项目中未设置）
  4. **HttpOnly Cookie**：防止 XSS 窃取 cookie（项目中未使用 Cookie，用 JWT Header，避免了一部分问题）
- 需要关注 `Whitelist.basicWithImages()` 是否允许了 `onerror`、`onload` 等事件属性

---

## 7. MyBatis 中的 N+1 查询问题与 SQL 优化

**老师可能的问法：** 你的文章列表查询，每篇文章都要关联查询作者信息和版块信息，这样会产生 N+1 查询问题，你怎么解决的？

**涉及代码：** [ArticleMapper.xml](src/main/resources/mapper/ArticleMapper.xml) 及 [ArticleExtMapper.xml](src/main/resources/mapper/extension/ArticleExtMapper.xml)

**答辩要点：**
- 文章的 `user` 和 `board` 关联对象是通过 MyBatis 的 `<resultMap>` + `<association>` 实现
- 如果是 `select` 属性单独查询（延迟加载），则会出现 N+1 问题
- 优化方案：
  1. 使用多表 JOIN 的 SQL，一次性查回所有数据（项目中 `selectDetailById` 使用了此方式）
  2. 使用 MyBatis 的 `association` 配合 `columnPrefix` 避免字段冲突
  3. 列表查询时先查文章 ID 列表，再批量查关联的用户/版块（batch query）
- 答辩时可以说项目中列表查询的关键 SQL 已通过 JOIN 优化，但部分关联查询仍有改进空间

---

## 8. 文件上传安全：MinIO 上传做了哪些防护？

**老师可能的问法：** 用户上传头像这个功能，你做了哪些安全控制？如果用户上传一个 webshell 怎么办？

**涉及代码：** [UserController.java:240-286](src/main/java/com/yb/forum/controller/UserController.java#L240-L286)、[MinioUtil.java](src/main/java/com/yb/forum/utils/MinioUtil.java)

**答辩要点：**
- 已做的防护：
  1. Spring Boot 的 `multipart.max-file-size: 5MB` 限制了文件大小
  2. MinIO 作为独立的对象存储服务，上传的文件不会在应用服务器上执行
- 未做或需加强的防护：
  1. **文件类型校验**：应通过 Magic Number（文件头魔数）校验，而非仅依赖文件扩展名或 MIME type
  2. **文件名处理**：应使用 UUID 重命名，防止路径穿越（`../../../etc/passwd`）
  3. **病毒扫描**：生产环境可集成 ClamAV
  4. **访问控制**：头像 Bucket 应设公开读，但应禁止列出目录
- 头像存在 MinIO 而非本地磁盘是好的设计，隔离了执行风险

---

## 9. Dify AI 智能体集成：流式响应解析和异常处理

**老师可能的问法：** 你接入了 Dify 作为智能助手，它的 streaming 模式你是怎么处理的？如果 Dify 服务挂了你的论坛会怎样？

**涉及代码：** [IDifyServiceImpl.java:40-184](src/main/java/com/yb/forum/services/impl/IDifyServiceImpl.java#L40-L184)

**答辩要点：**
- Dify Agent Chat API 只支持 streaming（SSE）模式，项目通过 `RestTemplate` 同步接收完整响应后，手动按行解析 `data:` 前缀的 SSE 事件流
- 这种实现的问题：
  1. **同步阻塞**：用户需等待 AI 完整回复，无法逐字展示（不符合聊天体验）
  2. **无超时/重试机制**：虽然配置了 `timeout: 30000`，但 RestTemplate 的超时需在 Bean 创建时设置 `connectTimeout` 和 `readTimeout`
  3. **调试日志过多**：大量 `System.out.println` 在生产环境不应使用
- 改进方向：使用 Spring WebFlux 的 `WebClient` 或 SSE 的 `SseEmitter` 实现真正的流式输出
- Dify 服务不可用时有 try-catch 兜底，返回友好的错误提示，不会导致论坛崩溃

---

## 10. 系统架构与安全性总结：前后端分离的跨域、权限和日志审计

**老师可能的问法：** 你的 CORS 配置允许了所有来源（`*`），管理员功能是如何做权限控制的？系统有没有日志审计？

**涉及代码：** [CorsConfig.java:15-21](src/main/java/com/yb/forum/config/CorsConfig.java#L15-L21)、[UserController.java:472-498](src/main/java/com/yb/forum/controller/UserController.java#L472-L498)

**答辩要点：**
- **CORS 跨域**：当前配置 `addAllowedOriginPattern("*")` + `allowCredentials(true)`，这在 Spring Boot 2.4+ 中是刻意分离的写法。但生产环境应限制为具体的前端域名，避免 CSRF 风险
- **权限控制**：
  1. 项目通过 `LoginInterceptor` 实现认证拦截
  2. 管理员权限在 `deleteUser` 方法中做了 `isAdmin == 1` 的校验
  3. 但部分管理员接口（如 `getAdminUserList`、`banUser`）未做管理员权限校验，这是一个安全漏洞
- **日志审计**：项目使用 SLF4J + Lombok 的 `@Slf4j`，关键操作（登录、发帖、删除）有日志记录。但缺少：
  1. 操作审计表（谁在什么时间做了什么操作）
  2. 敏感操作的 IP 记录
  3. AOP 切面实现统一的操作日志记录
- **SQL 注入防护**：使用 MyBatis 的参数化查询（`#{}`），天然防注入；`${}` 仅在 order by 等动态场景使用

---

## 额外提醒：老师可能追问的扩展性问题

| 方向 | 可能的问题 |
|------|-----------|
| **性能** | 如果论坛日活 10 万，你的系统瓶颈在哪？（DB 连接池、Redis 热 key、静态资源 CDN） |
| **扩展性** | 如果要支持多语言、国际化怎么办？（i18n + 数据库多语言字段设计） |
| **消息通知** | 有人回复了我的帖子，如何实时通知？（WebSocket / SSE 推送） |
| **全文搜索** | 帖子搜索目前用 SQL LIKE，数据量大时怎么办？（Elasticsearch / Lucene 分词） |
| **数据备份** | 数据库怎么备份和恢复？（全量 + binlog 增量，定时备份策略） |
| **部署运维** | 你的系统怎么部署上线？（Docker Compose / Nginx 反向代理 / HTTPS） |
