
# 删除文章 API 测试用例清单

> 接口：POST 删除文章  
> 数据来源：测试用例管理界面截图

---

## 测试用例列表

| 序号 | 用例名称 | 分组 | 运行结果 |
|:---:|----------|------|----------|
| 1 | 提供有效的文章ID-删除已存在的文章-期望返回200成功状态码 | 正向 | 失败200 |
| 2 | id参数值为0-删除文章接口传入0-期望返回400或404错误 | 负向 | 通过200 |
| 3 | id参数值为null-删除文章接口传入null-期望返回400错误 | 负向 | 失败200 |
| 4 | id参数值为不存在的文章ID-删除不存在的文章-期望返回404错误 | 负向 | 失败200 |
| 6 | id参数值为负数-删除文章接口传入负数id-期望返回400或404错误 | 负向 | 失败200 |
| 7 | id参数值为浮点数-删除文章接口传入小数id-期望返回400错误 | 负向 | 失败200 |
| 8 | id参数值为字符串类型-删除文章接口传入非数字id-期望返回400错误 | 负向 | 失败200 |
| 10 | id参数值为空字符串-删除文章接口传入空id-期望返回400错误 | 负向 | 失败200 |
| 11 | 缺失必填字段id-删除文章接口不传id参数-期望返回400错误 | 负向 | 失败200 |
| 12 | id参数值为1-删除文章接口传入最小有效正整数-期望返回200或404 | 边界值 | 通过200 |
| 14 | id参数值为极小值-删除文章接口传入极小整数-期望返回400或404错误 | 边界值 | 失败200 |
| 15 | id参数值为极大值-删除文章接口传入极大整数-期望返回400或500错误 | 边界值 | 失败200 |
| 16 | XSS攻击-删除文章接口id参数包含XSS payload-期望返回400或拒绝访问 | 安全性 | 失败200 |
| 17 | SQL注入攻击-删除文章接口id参数包含SQL注入payload-期望返回400或拒绝访问 | 安全性 | 失败200 |



## 简单分析

- **正常场景**：仅 1 个正向用例，且实际运行结果为“失败200”，说明正常删除也可能触发了业务错误。
- **异常输入覆盖**：较全面（0、null、不存在、负数、浮点、字符串、空、缺失字段）。
- **边界值**：包含极小值、极大值、最小有效正整数，但部分预期结果不统一（200/404/400/500）。
- **安全性**：有 XSS 和 SQL 注入，但缺少未授权/越权测试。


如需进一步优化或生成改进建议文档，可继续提供需求。