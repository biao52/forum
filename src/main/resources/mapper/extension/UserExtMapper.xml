<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yb.forum.dao.UserMapper">

    <select id="selectTotalCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_user WHERE deleteState = 0
    </select>

    <select id="selectUserGrowthLast7Days" resultType="java.util.Map">
        SELECT DATE_FORMAT(createTime, '%m-%d') as dateStr, COUNT(*) as count
        FROM t_user
        WHERE createTime >= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
          AND deleteState = 0
        GROUP BY DATE_FORMAT(createTime, '%m-%d')
        ORDER BY dateStr ASC
    </select>

    <select id="selectAllWithFilter" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM t_user
        WHERE deleteState = 0
        <if test="username != null and username != ''">
            AND username LIKE CONCAT('%', #{username}, '%')
        </if>
        ORDER BY createTime DESC
    </select>
</mapper>